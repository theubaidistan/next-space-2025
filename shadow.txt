generator client {
  provider = "prisma-client-js"
   previewFeatures = ["someFeature"]  // optional
}

datasource db {
  provider          = "postgresql"
  url               = env("POSTGRES_PRISMA_URL")
}